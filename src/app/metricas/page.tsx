// Página de métricas — renderiza o MetricsLayout com gráficos e consolidado de tempo.
import { MetricsLayout } from '@/components/metrics/MetricsLayout';

export default function MetricasPage() {
  return <MetricsLayout />;
}
